# No application environment keys have been confirmed yet.
# Add confirmed KEY= entries with purpose, required/optional and server/client scope.
# Secret values belong in Doppler, never in this file.
# Setup and CI boundaries: docs/operations/development-foundation-status.md
